{
  "name": "story-engine-desktop",
  "version": "0.2.0",
  "private": true,
  "description": "StoryEngine 桌面壳：Electron 主进程 in-process 起独立 server（connect 全路由 + Mastra agent），窗口连 localhost。",
  "type": "module",
  "main": "main.mjs",
  "scripts": {
    "start": "electron .",
    "pack:dir": "echo '请用 ./build-desktop.sh（原因：必须先 pnpm deploy 到 staging，从工作区直接 electron-builder --dir 会打出坏包）' && exit 1",
    "dist:mac": "bash ./build-desktop.sh --mac",
    "dist:win": "bash ./build-desktop.sh --win",
    "test": "node --check main.mjs && node --check server-port.mjs && bash -n ./build-desktop.sh && bash ./build-desktop.test.sh"
  },
  "dependencies": {
    "@actalk/story-engine-ui": "workspace:*"
  },
  "devDependencies": {
    "electron": "^43.0.0",
    "electron-builder": "^26.15.3"
  }
}
